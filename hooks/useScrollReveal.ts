"use client";

/**
 * Deprecated — scroll reveals now handled by Framer Motion whileInView.
 * Kept to avoid breaking existing imports during migration.
 */
export function useScrollReveal() {}
export default useScrollReveal;